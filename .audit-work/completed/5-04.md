## Plain English

This audit item flagged that a safety rule (preventing duplicate Fresha service entries per professional) was missing from the database. On investigation, the rule already existed — it was added when the database schema was first built. No change was needed.

## Technical Summary

Audit item #5-04 is a false positive. The unique constraint `services_professional_fresha_variation_uq UNIQUE (professional_id, fresha_variation_id)` on `site.services` already exists in the baseline migration (`supabase/migrations/20260403000000_v2_baseline.sql`, lines 678–679). No subsequent migration drops or modifies it. The checkbox in `pilot-stage-1.md:1369` was ticked to close the item; no SQL migration was created.

## Decisions Made

- Confirmed redundant against baseline migration: checked all `.sql` files in `supabase/migrations/` for the constraint name — only one ADD CONSTRAINT hit, no DROP CONSTRAINT hits.
- Chose not to write a no-op migration: a duplicate ADD CONSTRAINT would fail at deployment and would pollute migration history.

## Notes

The audit scanner likely flagged this by reading only `FreshaServiceSyncService.php` without cross-referencing the full migration history. The parallel Square constraint (`services_professional_square_variation_uq`) is defined in the same block (line 675–676), suggesting the pattern was intentional from the start.
