---
item_id: '#8-05'
title: '`auth_user_id` echoed in ProfessionalResource'
source: pilot-stage-1.md
completed_at: '2026-05-05T00:00:00+00:00'
outcome: done
---

# #8-05 — `auth_user_id` echoed in ProfessionalResource

## Plain English

The Professional resource shapes were refactored by audience in commit `72e058e refactor(api): canonical Professional resource shapes per audience`. The remaining `auth_user_id` fields in `ProfessionalDashboardResource` (professional's own session) and `ProfessionalStaffResource` (identity verification for staff) are intentional. The public resource correctly excludes it. No change was warranted.

## Technical Summary

Resolved by prior commit `72e058e`. The orchestrator blocked with "no new commits ahead of base ref" because no unsafe exposure remained after the resource shape refactor. Marked done manually after verifying the remaining usages are audience-appropriate.

## Notes

- `ProfessionalPublicResource` — correctly excludes `auth_user_id`
- `ProfessionalDashboardResource` — intentionally includes it (professional's own data)
- `ProfessionalStaffResource` — intentionally includes it (staff identity verification, per inline comment)
